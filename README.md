# Simple docker project

Minimal docker project, packaged in Docker.

## How to run

```bash
docker-compose up --build

After the app will be reachable at http://localhost:5001
Healthcheck: http://localhost:5001/healthcheck
